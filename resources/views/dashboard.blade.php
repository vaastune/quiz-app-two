@extends('layouts.app')

@section('content')
    <p>Welcome to the Dashboard! Manage quizzes and results here.</p>
@endsection

<!-- Use only one layout approach. If you choose 'x-app-layout', remove the @extends part. -->

<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900 dark:text-gray-100">
                    <h3 class="text-lg font-medium">Quizzes</h3>
                    @if ($quizzes->isEmpty())
                        <p>No quizzes available at the moment.</p>
                    @else
                        <ul>
                            @foreach ($quizzes as $quiz)
                                <li class="mb-2">
                                    <a href="{{ route('quizzes.show', $quiz->id) }}" class="text-blue-500 hover:underline">
                                        {{ $quiz->title }} (Category: {{ $quiz->category }})
                                    </a>
                                </li>
                            @endforeach
                        </ul>
                    @endif
                </div>
            </div>
        </div>
    </div>
</x-app-layout>


{{-- <x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900 dark:text-gray-100">
                    {{ __("You're logged in!") }}
                </div>
            </div>
        </div>
    </div>
</x-app-layout> --}}
